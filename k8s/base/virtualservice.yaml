apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: ledger-api
  labels:
    app.kubernetes.io/name: ledger-api
spec:
  hosts:
    - ledger-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: ledger-api
            port:
              number: 80
          weight: 100
